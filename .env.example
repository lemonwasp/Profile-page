# This portfolio currently requires no secrets.
#
# If environment variables are added later:
# - keep secrets in Vercel/GitHub environment settings, never in source control;
# - remember that NEXT_PUBLIC_* values are exposed to the browser;
# - document variable names here with empty placeholder values only.
